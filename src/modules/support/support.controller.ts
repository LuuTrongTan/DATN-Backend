import { Response } from 'express';
import { AuthRequest } from '../../types/request.types';
import { pool } from '../../connections';
// Flow support ticket đã bị loại bỏ. Controller giữ trống để tránh lỗi import.
export const placeholderSupportController = () => {
  return 'support tickets feature is disabled';
};

